server:
  port: ${PORT:8080}
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/github-actions-demo
    username: github-actions-demo
    password: github-actions-demo
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
